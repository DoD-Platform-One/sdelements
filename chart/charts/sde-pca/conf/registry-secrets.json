{
    "auths":{
        {{ default "docker.sdelements.com" .Values.imageRegistry | quote }}:{
            "username": {{ required "A username for the image registry is required, try --set Values.imageRegistryUsername=user" .Values.imageRegistryUsername | quote }},
            "password": {{ required "A password for the image regestry is required, try --set Values.imageRegistryPassword=password" .Values.imageRegistryPassword | quote }},
            "email": {{ default "support@sdelements.com" .Values.imageRegistryEmail | quote }},
            "auth": {{ printf "%s:%s" .Values.imageRegistryUsername .Values.imageRegistryPassword | b64enc | quote }}
         }
     }
}